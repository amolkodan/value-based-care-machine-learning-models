services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: vbc
      POSTGRES_PASSWORD: vbc
      POSTGRES_DB: vbc
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U vbc -d vbc"]
      interval: 5s
      timeout: 3s
      retries: 20
